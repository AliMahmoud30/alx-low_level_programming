#include "main.h"
/**
 * mul - Check if the given character is an uppercase letter.
 * @a: number1 to test
 * @b: number2 to test
 *
 * Return: multiplies two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}

